version: "1.2.6"

endpoints:
  azureOpenAI:
    groups:
      - group: phi4          
        apiKey: ${AZURE_PHI4_KEY}
        baseURL: ${AZURE_PHI4_BASE_URL}
        serverless: true
        version: 2024-05-01-preview 
        models:
          Phi-4: true            
